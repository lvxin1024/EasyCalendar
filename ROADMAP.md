# EasyCalendar Roadmap

## 目标

普通用户下载 GitHub Release 安装包后即可使用完整的本地功能。除可选的云同步服务部署外，用户不需要安装 Python、Flutter 或 Node.js，不需要编辑配置文件，也不需要设置环境变量。所有运行时配置都应能在 App 内完成。

状态约定：`已完成`、`进行中`、`待开始`、`受外部条件阻塞`。每个任务必须满足验收标准并通过相关自动化测试后才能标记为已完成。

## 实施顺序

```text
设备身份 -> 服务边界/能力发现 -> 首次启动向导
         -> Release CI -> 干净安装/覆盖升级
         -> Core 交付边界 -> 安全存储发布验收
         -> 其余设置与产品化任务
```

P0.2 先于 P0.1 实施，因为它可以独立消除多台默认安装使用相同设备 ID 的数据风险。P0.3 与 P0.4 必须连续完成：能构建产物不等于产物可安装、可升级。

## P0：Release / 开箱即用阻塞项

### P0.1 拆分服务配置并做能力发现

- 状态：已完成（2026-08-18）。
- 当前缺口：同一个“API 地址”同时被同步、网址订阅和远程导入导出使用；Cloudflare Worker 只实现同步接口，Python Core 又不实现同步。
- 内容：设置中明确区分“同步服务”和“功能服务”，或提供统一网关；为健康检查、能力发现和鉴权检查建立客户端模型；页面根据服务能力启用或降级。
- 验收：按 README 配置不再出现必然的 404；保存前可测试连接；错误能区分网络、TLS、401、版本和能力不兼容；本地模式不要求 token。
- 实现：拆分并持久化同步服务地址、功能服务地址及各自的安全令牌；Core 支持可选 Bearer 鉴权；设置页检测 health、capabilities 和鉴权并区分 DNS、TLS、超时、401、版本及能力错误；订阅和远程 ICS 页面会自动探测并按能力禁用，避免向错误服务发送业务请求。

### P0.2 自动生成并持久化设备身份

- 状态：已完成（2026-08-18）。
- 当前缺口：所有默认安装使用 `my-easycalendar-client`，用户必须理解并手工避免冲突。
- 内容：首次启动生成唯一且稳定的设备 ID；增加可读设备名称；迁移空值、非法值和旧默认值；技术 ID 只放在高级设置中；允许经过风险确认后重新生成。
- 验收：两个全新安装获得不同 ID；重启和覆盖升级后 ID 不变；普通用户无需填写 ID；重新生成不改写已有 outbox，旧、新 ID 均可按批次上传；有单元测试覆盖生成、迁移、持久化和重置。
- 实现：默认构建参数留空，启动时在 Repository 初始化前生成 `device-<UUID>`；Controller 将设备 ID 和名称写入 `app_settings`，并迁移空值、非法值及旧固定默认值；设置页将 ID 放入高级区域，提供复制和带确认的重建操作。

### P0.3 建立多平台 Release 流水线

- 状态：进行中。
- 依赖：P0.2。
- 当前缺口：三平台 tag 工作流已定义，但仓库签名 Secrets 尚未配置，也未完成首次真实 Runner 构建、公证和安装验证。
- 内容：Git tag 自动构建 macOS、Windows、Android 安装产物；配置正式签名；生成版本说明、SHA-256 和必要的调试符号。
- 验收：Release 包含 macOS 签名并公证的 DMG/PKG、Windows 签名安装包、Android release 签名 APK（AAB 可选）；签名材料只存在 CI Secrets；产物版本与 tag 一致。
- 进度：Android Release 已移除 debug 签名回退；tag 工作流已定义签名 Android APK/AAB、签名 Windows 安装器/便携包、签名并公证 macOS DMG、调试符号和 SHA-256。待配置仓库 Secrets 并在三平台 Runner 实际验证后完成。

### P0.4 干净安装和覆盖升级验收

- 状态：待开始。
- 依赖：P0.3。
- 当前缺口：开发环境可以运行，但尚未证明安装包脱离源码和工具链后可用。
- 内容：建立各平台 clean-install 和 upgrade smoke test；覆盖首次启动、本地 CRUD、设置持久化、安全存储和数据库迁移。
- 验收：全新环境无需 `config/client.json`、Python、Flutter、Node 即可启动和本地使用；覆盖升级不丢数据库、同步令牌和 AI Key；失败有可复现记录。

### P0.5 收敛 Python Core 与客户端的运行时边界

- 状态：已完成（客户端本地能力）；ICS 文件传输、网址订阅、条件抓取、只读日历刷新和基础中文规则解析均由 Flutter 提供。Python Core 保留为可选兼容 API。
- 依赖：P0.1。
- 当前缺口：复杂自然语言规则仍需要 AI Provider 或手动校对；Python Core 的兼容 API 不再是安装包运行前提。
- 内容：选择并完成一种交付方式：移植到 Flutter、本地打包并托管 Core sidecar，或连接独立功能服务；同步服务继续保持可选。
- 验收：用户不需要手工启动后台进程；App 可显示组件状态和错误；README 宣传的功能与安装包实际能力一致（已满足）。

### P0.6 验证 Release 环境下的安全存储

- 状态：待开始。
- 依赖：P0.3、P0.4。
- 当前缺口：代码已使用 `flutter_secure_storage`，但缺少签名安装包的端到端验收。
- 内容：核对 macOS Keychain entitlement、Windows Credential Manager/包身份和 Android Keystore/备份策略；审计日志、SQLite 和备份边界。
- 验收：AI Key 和同步令牌可新增、替换、清除并在重启/升级后读取；secret 不进入 SQLite、JSON 备份、日志或崩溃报告；三平台真机通过。

## P1：设置闭环与产品化

### P1.1 首次启动向导与配置诊断

- 状态：待开始。
- 内容：首次启动提供“仅本地使用”和“连接已有服务”；同步和 AI Provider 提供独立测试；诊断结果可复制且自动脱敏。
- 验收：配置失败不阻塞本地数据；用户不需要先阅读部署文档才能进入 App。

### P1.2 时区、语言和日期偏好改为运行时设置

- 状态：待开始。
- 内容：默认跟随系统；设置中允许覆盖时区、语言、每周起始日和 12/24 小时制。
- 验收：日历、解析、提醒、Widget、导入导出使用同一配置；修改偏好不会错误改写已有 UTC 数据。

### P1.3 隐藏技术性 Collection ID

- 状态：待开始。
- 内容：普通界面只选择默认日历；ID 由 App 生成维护；连接已有数据时通过列表、配置码或导入选择。
- 验收：常规流程不要求复制内部 ID；高级区域仍可查看和复制；切换默认日历不误迁移已有事项。

### P1.4 完善 AI Provider 配置向导

- 状态：待开始。
- 内容：增加常见 OpenAI-compatible/Ollama 预设、模型发现、编辑弹窗内连接测试、超时/代理/常用参数设置。
- 验收：尚未保存的 Key 可以用于测试；Key 可替换或清除但永不完整回显；模型发现失败仍可手填。

### P1.5 接入真实系统通知与权限设置

- 状态：待开始。
- 内容：替换 InMemory adapter；展示权限状态；提供申请权限、跳转系统设置和测试通知。
- 验收：创建、改期、关闭提醒、重启恢复和时区变化正确更新平台通知；拒绝权限不阻塞事项保存。

### P1.6 客户端备份、迁移和故障恢复

- 状态：待开始。
- 内容：schema 升级前自动备份；设置页提供备份查看、恢复和清理；非敏感设置支持独立导入导出。
- 验收：迁移失败保留原库并提供恢复入口；secret 和设备 ID 默认不导出。

### P1.7 应用内版本与更新入口

- 状态：待开始。
- 内容：关于页展示版本、构建号、平台和数据 schema；检查 GitHub Release 并打开对应安装包。
- 验收：更新说明与当前版本可比较；后续自动更新必须验证签名且不绕过系统安全机制。

### P1.8 统一应用身份与安装元数据

- 状态：待开始。
- 内容：统一应用名、bundle/application ID、发布者、图标、版本规则和安装路径。
- 验收：Windows 卸载项、macOS About/签名信息、Android 标签一致；升级不会因标识变化创建第二份数据目录。

### P1.9 补齐客户端 CI 质量门禁

- 状态：进行中。
- 内容：PR 运行 Flutter analyze/test，并至少编译 macOS、Windows、Android release；补充安装和设置相关测试。
- 验收：Python、Worker、Flutter 测试全部是合并门禁；首次启动、设置持久化、设备 ID 唯一性、能力发现和安全存储有覆盖。
- 进度：push/PR 已运行固定 Flutter 版本的依赖解析、静态分析和完整客户端测试；待补三平台原生 Release 编译门禁。

## 中低优先级与暂缓项

- Parser：补充复杂时间范围、自然语言时长和重复规则 fixture。
- 标签：编辑时列出已有标签供复用。
- 云端自动备份：实现 `auto_backup_before_migrate`；不阻塞纯本地安装包。
- 暂缓：Importer SDK、Google/Microsoft/飞书适配、Docker Compose、Cloudflare Cron/R2 备份、Windows Widget。

## 变更记录

- 2026-08-18：以“设置内完成配置、Release 安装包开箱即用”为标准建立公开路线图；开始 P0.2。
- 2026-08-18：完成 P0.2；新增自动设备身份、可读设备名称、旧默认值迁移、高级重建入口和测试。
- 2026-08-18：开始 P0.1；完成同步服务与功能服务地址拆分。
- 2026-08-18：P0.1 增加独立功能服务凭据；Core 配置 token 后保护业务 API，未配置时保持本机零配置使用。
- 2026-08-18：P0.1 增加同步/功能服务连接检测；验证健康、API 版本、服务能力和令牌，不执行数据写入。
- 2026-08-18：完成 P0.1；订阅与远程 ICS 按已发现能力启用或降级，本地 JSON 备份不受功能服务影响。
- 2026-08-18：推进 P0.5；ICS 文件解析、预览、去重、导入和导出迁入 Flutter 客户端，不再要求功能服务。
- 2026-08-18：P0.5 增加本地订阅生命周期；只读 Collection、编辑启停、软删除和同步 outbox 均由 Flutter 本地仓库管理。
- 2026-08-18：P0.5 将网址订阅页面和抓取迁入 Flutter；支持 ETag/Last-Modified、SHA-256 变更标识、稳定事项 ID、刷新日志和 10 MB 响应限制。
- 2026-08-18：P0.5 完成客户端本地边界；无 AI Provider 时助手使用中文规则解析器，Python Core 降为可选兼容服务。
- 2026-08-18：开始 P0.3；Android Release 改为强制使用独立签名配置，禁止 debug key 产出正式包。
- 2026-08-18：开始 P1.9；现有 Tests 工作流加入 Flutter analyze/test 门禁。
- 2026-08-18：P0.3 增加 tag 驱动的三平台签名发布工作流；待注入证书并完成首次真实 Release 验证。
